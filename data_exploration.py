import time
import os
import pandas as pd
import seaborn as sns
import utils.helper_exploration as exploration
import utils.common as common


class DataExploration:

    def dataExploration(self, data_clean):
        print('Start Data Exploration:')

        #exploration.check_target_distribution(data_clean)
        #exploration.dist_numeric(data_clean)
        #exploration.count_catagorical(data_clean)
        #exploration.explore_marital_status(data_clean)
        #exploration.explore_relationship(data_clean)
        #exploration.explore_race(data_clean)
        #exploration.explore_occupation(data_clean)
        #common.profiling(data_clean)

        print('Done Data Exploration...')



